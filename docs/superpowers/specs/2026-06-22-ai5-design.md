# Slice AI5 — Deterministic AI: LOCKSTEP + ROLLBACK over AI decisions (the moat headline) — Design

> Autonomous-session spec. Verifiable headlessly on Windows+Vulkan + Mac+Metal. The FIFTH slice of the DETERMINISTIC
> AI flagship (GitHub issue #28, slice-tag **AI**). AI1–AI4 built the deterministic NPC (decision tree, environment
> queries, line-of-sight, the agent composing them in the gameplay tick). AI5 is the moat HEADLINE a float engine
> cannot do: prove the NPC AI is true cross-platform LOCKSTEP + ROLLBACK-replayable. Two peers fed ONLY a deterministic
> INPUT stream (the player's moves — which drive each agent's perception → decision → query → path → act) re-simulate
> to a BIT-IDENTICAL AI world (every agent's chase/patrol decision, chosen destination, corridor, and position); a
> mispredicted player move that flips a BT decision (chase↔patrol) is corrected exactly by rolling back to a snapshot
> and re-simulating. The AI rig state IS the replayable state — a determinism PROPERTY of the AI1–AI4 integer pipeline.
> PURE CPU, 0 backend symbols, NO new shader/RHI (the FPX5/VD5/IK5 lockstep convention). **APPENDS to `engine/ai/ai.h`**
> (`hf::ai`, AI1–AI4 byte-frozen — append-only). Branch: `slice-ai5`. See [[hazard-forge-ai-roadmap]],
> [[hazard-forge-docs-style]], [[hazard-forge-build-env-vcvars]], [[hazard-forge-metal-showcase-gate]].

**Goal:** Append to `engine/ai/ai.h`: `struct AiSimState { std::vector<AiAgent> agents; FxVec3 playerPos; int tick; }`
(the agents + the player position = the replayable mutable state; the navmesh + the decision tree + the blockers are
CONST scene topology, carried as params / re-supplied at restore, NOT snapshotted — the IK5/VD5 const-topology lesson),
`struct AiCommand { int tick; FxVec3 playerDelta; }` (the per-tick player move — the input that drives the AI),
`SimAiTick(state, navmesh, tree, blockers, commands, tick)` (apply the player move, then `StepAi` — the frozen AI4
pass), `SnapshotAi`/`RestoreAi` (bit-exact round-trip of the mutable state), `AiStatesEqual`, `RunAiLockstep` (two
peers from the same init + command stream → `AiStatesEqual` every tick; returns the converged authority state), and
`RunAiRollback` (advance to `rollbackAt`, snapshot, mispredict ≤3 ticks with a WRONG player-move stream that flips an
agent's chase↔patrol decision + changes its destination + corridor, restore, re-sim the correct stream; corrected ==
authority + the mispredict diverged). Add `--ai5-lockstep-shot` (Vulkan, pure-CPU proof) / `--ai5-lockstep` (Metal)
rendering the converged authority world. Bake the **strict-zero** golden `ai5_lockstep`.

## Design call: the FPX5/VD5/IK5 lockstep mold; the player move is the input, the agents are the state
The lockstep/rollback machinery is proven six times (FPX5, VD5, VH5, AC5, IK5, plus the warmhull/persist twins). AI5
reuses the SHAPE verbatim:
- **The replayable state = the agents + the player position.** The player's moves are the INPUT (`AiCommand`); each
  agent's full AI state (blackboard, decision, destination, corridor, position) is RE-DERIVED each tick by the frozen
  `StepAi` from the (moved) player + the const navmesh/tree/blockers. The navmesh + tree + blockers are CONST topology
  — re-supplied at restore, NOT in the snapshot (the IK5 base-pose / VD5 hulls discipline; snapshotting them is
  wasteful and the restore re-seeds them).
- **`SimAiTick` = apply the player move → `StepAi`.** Deterministic by construction (the AI4 fixed-order integer pass).
  Two peers running the same `SimAiTick` over the same player-move stream are byte-identical; rollback restores the
  exact `AiSimState` and re-sims.
- **The snapshot captures the agents + player + tick.** `AiStatesEqual` compares them in fixed order (the `DigestAgents`
  currency). `RestoreAi` restores them; the const navmesh/tree/blockers are the caller's params.
- **The mispredict is a player move that flips a DECISION.** The WRONG stream moves the player behind a blocker (or
  out of chase range) at the rollback tick, so an agent that should CHASE instead PATROLs (the BT switches on
  perception) → the mispredicted state diverges across the agent's decision AND destination AND corridor AND position;
  the rollback corrects it exactly. This is the "deterministic replayable AI a float engine can't do" headline — a
  float AI's chase/patrol decision is not bit-reproducible, so it can't lockstep.
- **Determinism by construction:** the cross-backend zero-diff golden of the converged authority world IS the lockstep
  evidence (both backends run the same CPU `SimAiTick`). PURE CPU — no GPU, no shader, no TDR.

## Reuse map (file:line)
- **`engine/game/verdict.h`:** `RunVerdictLockstep` (`:~873`) + `RunVerdictRollback` (`:~901`) + `ClonePeer` (`:~847`)
  — the control flow COPIED VERBATIM with `SimAiTick` substituted; `DigestSnapshot` (`:~1139`, the state-digest shape).
- **`engine/sim/fpx.h`:** `RunLockstep` / `RunRollback` (`:~567/583`, the canonical command-stream mold).
- **`engine/ai/ai.h` (AI4, append target):** `AiAgent`, `StepAi`, `DigestAgents` (the per-tick step + the state
  digest). The IK5 spec (the append-only minimal-state lockstep shape — re-supply the const topology at restore).
- **A recent pure-CPU lockstep slice** (the IK5 `--ik5-lockstep` / VD5 `--vd5-net` showcase + the converged-state
  2D-side-view golden + the proof-line structure) — AI5 mirrors it EXACTLY (the converged AI world viz).

## Design decisions (locked)
1. **APPEND to `engine/ai/ai.h`** (`hf::ai`, AI1–AI4 byte-frozen — do NOT edit existing lines): `AiSimState` /
   `AiCommand` / `AiSnapshot` / `SimAiTick` / `SnapshotAi` / `RestoreAi` / `AiStatesEqual` / `RunAiLockstep` /
   `RunAiRollback`. All pure CPU, header-only, deterministic. The const navmesh/tree/blockers are passed as params to
   every `Sim*`/`Run*` (NOT stored in the snapshot).
2. **`--ai5-lockstep-shot <out>` (Vulkan, pure-CPU proof) AND `--ai5-lockstep` (Metal) — WIRE BOTH (grep your OWN
   visual_test.mm for `--ai5-lockstep` BEFORE reporting DONE).** Both build the AI4 scene (navmesh + agents + player +
   blockers) + the SAME player-move stream, run `RunAiLockstep`, and render the converged authority world (the
   AI4-style 2D top-down: agents by state + paths + player) → bit-identical cross-backend BY CONSTRUCTION (strict
   zero-differing-pixel). Golden = `tests/golden/metal/ai5_lockstep.png` (CONTROLLER-baked).
3. **PROOFS (exact stdout lines, fail loudly):**
   - `ai5-lockstep: {ticks:<K>, agents:<A>} authority==replica BIT-IDENTICAL (decisions+queries+paths) from inputs ONLY`
   - `ai5-lockstep determinism: two RunAiLockstep runs BYTE-IDENTICAL`
   - `ai5-lockstep rollback: corrected==authority BIT-EXACT; pre-rollback mispredict DIFFERED (BT decision + dest + corridor diverged)`
   - `ai5-lockstep: snapshot/restore round-trip BIT-EXACT (agents+player+tick); strict cross-vendor 0.0000 expected at the bake`
   - Golden discipline: do NOT commit a Mac golden (controller bakes `ai5_lockstep.png`). Existing 269 goldens
     UNTOUCHED.
4. **Cross-backend bar:** strict **0.0000** (pure-CPU both backends → bit-identical converged-world viz).
5. **Tests — NEW `tests/ai_lockstep_test.cpp` (pure CPU, ASan-clean), ctest +1:** `SnapshotAi`→`RestoreAi` round-trip
   bit-exact; `RunAiLockstep` two-peer convergence (whole AI state equal each tick); `RunAiRollback` corrected==authority;
   the mispredict genuinely diverged (the pre-rollback state — an agent's decision/dest/corridor — differs from the
   authority); a no-op mispredict (identical stream) does NOT diverge (the flag is meaningful); two runs byte-identical.
   Register in `tests/CMakeLists.txt`.
6. **Introspect.** Add EXACTLY `ai5-lockstep` (features) + `--ai5-lockstep-shot` (showcases) + update
   `tests/introspect_test.cpp`. CONTROLLER rebakes the JSON goldens.

## RHI seam additions (summary)
- **None.** PURE-CPU appended logic over the frozen AI4 step. Report the seam: `ai.h` APPEND-ONLY (AI1–AI4
  byte-unchanged); NO shader, NO RHI, NO `hf_gen_msl`; verdict/nav/fpx + ALL shaders + the 269 existing goldens
  BYTE-UNCHANGED. CONFIRM a clean Mac metal_headless build.

## Out of scope (YAGNI)
Network transport / real packets (the in-process two-peer determinism harness, the VD5 convention). FABRIK-style
temporal smoothing of decisions. The lit render (AI6). AI5 claims ONLY: the NPC AI state is bit-identical across two
peers from the player input stream AND rollback-replayable (a mispredicted decision corrected exactly), pure CPU,
strict-zero cross-vendor — deterministic replayable game AI.

## Verification gate (controller)
1. `ctest --preset windows-msvc-debug -R "ai_lockstep|ai_agent|ai_los|ai_query|ai_tree|introspect"` green. Clean under
   `windows-msvc-asan`.
2. `--ai5-lockstep-shot` on Vulkan: all 4 proofs + exit 0. VERIFY the converged-world viz is coherent (agents by state
   + paths + player, the AI4 look, no garbage).
3. Metal: `visual_test --ai5-lockstep` → `tests/golden/metal/ai5_lockstep.png`; two runs DIFF 0.0000; cross-vendor
   STRICT 0.0000. Confirm `visual_test.mm` in the diff; confirm a CLEAN Mac metal_headless build.
4. **Render-invariance:** ONLY `ai5_lockstep.png` added; the other 269 byte-identical (+ controller introspect rebake).
5. Introspect: exactly `+ai5-lockstep` + `--ai5-lockstep-shot`.
6. Seam grep: `ai.h` APPEND-ONLY; NO shader/RHI; verdict/nav/fpx + ALL shaders + existing goldens BYTE-UNCHANGED.
   **Then AI6 (the lit 3D NPC render capstone — the public AI sample → consolidation → close issue #28).**
