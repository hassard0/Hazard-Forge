// GENERATED by tools/material_codegen from a *.mat.json material graph (Slice AV).
// DO NOT EDIT — regenerate from the source JSON. Shares the PBR core in pbr_core.hlsli
// with the hand-written lit_pbr.frag.hlsl (identical FrameData / varyings / bindings).
// (Generated under shaders/generated/, so the PBR core include is one dir up.)
#include "../pbr_core.hlsli"

float4 main(PSInput i) : SV_Target {
    float2 n1 = i.uv;
    float4 n2 = gBase.Sample(gBaseSmp, n1);
    float n3 = n2.x;
    float n4 = n2.y;
    float n5 = 1.0 - n4;
    float n6 = n2.z;
    float n7 = pow(1.0 - hfNoV(i), 2.0);
    float n8 = 3.0;
    float n9 = pow(n7, n8);
    float n10 = saturate(n9);
    float n11 = 0.40000001;
    float3 n12 = float3(n10, n6, n11);

    float3 baseColor = n12;
    float  metallic  = n3;
    float  roughness = n5;
    float3 emissive  = float3(0.0, 0.0, 0.0);
    return hfShadePBR(i, baseColor, metallic, roughness, emissive);
}
